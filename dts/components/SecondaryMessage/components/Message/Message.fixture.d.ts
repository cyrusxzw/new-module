export namespace MessageFixture {
  const className: string;
  const copy: string;
  const heading: string;
  const id: string;
  namespace link {
    const title: string;
    const url: string;
  }
}
