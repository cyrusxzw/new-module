export namespace SubNavFixture {
  const heading: string;
  const id: string;
  const isSelect: boolean;
  const links: {
    id: string;
    style: string;
    children: string;
    url: string;
  }[];
  const theme: string;
}
