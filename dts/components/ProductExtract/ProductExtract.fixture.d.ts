export namespace ProductExtractFixture {
  const dataTestRef: string;
  namespace product {
    namespace image {
      const altText: string;
      const small: string;
    }
    const name: string;
    const url: string;
  }
  const works: string;
}
