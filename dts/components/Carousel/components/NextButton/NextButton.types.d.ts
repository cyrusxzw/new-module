import type { MouseEvent } from 'react';
import type { Themes } from '~/types';
declare type NextButtonProps = {
  isActive?: boolean;
  onClick?: (event: MouseEvent) => void;
  theme?: Themes;
};
export type { NextButtonProps };
