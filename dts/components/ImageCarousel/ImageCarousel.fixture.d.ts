export namespace ImageCarouselFixture {
  const autoplaySpeed: number;
  const hasAutoplay: boolean;
  const slides: {
    caption: string;
    image: {
      altText: string;
      small: string;
      medium: string;
      large: string;
    };
  }[];
  const theme: string;
}
