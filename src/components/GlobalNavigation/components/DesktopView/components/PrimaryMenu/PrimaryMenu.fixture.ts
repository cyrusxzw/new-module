import { PrimaryMenuProps } from './PrimaryMenu.types';

const PrimaryMenuFixture: PrimaryMenuProps = {
  onClose: () => console.log('PrimaryMenu: onClose'), // eslint-disable-line no-console
};

export { PrimaryMenuFixture };
