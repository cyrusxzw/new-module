import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import { Hyperlink } from './Hyperlink';

<Meta title="Components/Hyperlink" component={Hyperlink} />

# Hyperlink

<Canvas>
  <Story name="Base component">
    <Hyperlink
      isAlternate={knobs.boolean('isAlternate', false, 'Presentation')}
      kind={knobs.select(
        'kind',
        [
          'External Button Link',
          'External No Icon Button Link',
          'External No Icon Link',
          'External Text Link',
          'Internal Button Link',
          'Internal No Icon Button Link',
          'Internal No Icon Link',
          'Internal Text Link',
        ],
        'Internal Text Link',
        'Presentation',
      )}
      textAlign={knobs.select(
        'textAlign',
        ['left', 'center', 'right'],
        'center',
        'Presentation',
      )}
      theme={knobs.select('theme', ['dark', 'light'], 'dark', 'Presentation')}
      title="Hyperlink Title"
      url={knobs.text('url', '/', 'Copy')}
    >
      {knobs.text('children', 'Lorem ipsum', 'Copy')}
    </Hyperlink>
  </Story>
</Canvas>

## API

<ArgsTable of={Hyperlink} />
