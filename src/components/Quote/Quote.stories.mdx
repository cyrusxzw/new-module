import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import { Quote } from './Quote';
import { QuoteFixture } from './Quote.fixture';

<Meta title="Components/Quote" component={Quote} />

# Quote

<Canvas>
  <Story name="Base component">
    {(() => {
      const author = knobs.text('author', QuoteFixture.author, 'Copy');
      const content = knobs.text('children', QuoteFixture.content, 'Copy');
      const theme = knobs.select(
        'theme',
        ['dark', 'light'],
        'dark',
        'Presentation',
      );
      return <Quote author={author} content={content} theme={theme} />;
    })()}
  </Story>
</Canvas>

## API

<ArgsTable of={Quote} />
