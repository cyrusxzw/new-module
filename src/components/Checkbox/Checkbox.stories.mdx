import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import { Podium } from '~/components/Podium';
import { Checkbox } from './Checkbox';

<Meta title="Form Elements/Checkbox" component={Checkbox} />

# Checkbox

<Canvas>
  <Story name="Base component">
    <Podium>
      <Checkbox
        content={knobs.text(
          'content',
          'Subscribe to communications about Aesop products, services, stores, events and matters of cultural interest.',
          'Content',
        )}
        errorMessage={knobs.text('errorMessage', '', 'Content')}
        isEnabled={knobs.boolean('isEnabled', true, 'Presentation')}
        theme={knobs.select('theme', ['dark', 'light'], 'dark', 'Presentation')}
      />
    </Podium>
  </Story>
</Canvas>

## API

<ArgsTable of={Checkbox} />
