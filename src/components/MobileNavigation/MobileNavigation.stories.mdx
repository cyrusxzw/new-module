import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import { StorybookGridOverlay } from '~.storybook/components';
import { Modal } from '~/components/Modal';
import { MobileNavigation } from './MobileNavigation';
import { MobileNavigationFixture } from './MobileNavigation.fixture';
import { Video } from '~/components/Video';
import { HeroBanner } from '~/components/HeroBanner';
import { HeroBannerFixture } from '~/components/HeroBanner/HeroBanner.fixture';

<Meta title="Components/MobileNavigation" component={MobileNavigation} />

# MobileNavigation

<Canvas>
  <Story name="Base component">
    {(() => {
      const [cartOpen, setCartOpen] = React.useState(false);
      const handleOnCartClick = () => setCartOpen(true);
      const handleOnCartCloseClick = () => setCartOpen(false);
      const [loginOpen, setLoginOpen] = React.useState(false);
      const handleOnLoginClick = () => setLoginOpen(true);
      const handleOnLoginCloseClick = () => setLoginOpen(false);
      const theme = knobs.select(
        'theme',
        ['dark', 'light'],
        'dark',
        'Presentation',
      );
      const implementation = {
        gridOverlay: {
          isActive: knobs.boolean('Show grid overlay', false, 'Implementation'),
          hasInvertedColours: knobs.boolean(
            'Invert grid overlay colours',
            false,
            'Implementation',
          ),
        },
      };
      return (
        <>
          <MobileNavigation
            closedTheme="light"
            isVisuallyObstructed={cartOpen || loginOpen}
            items={MobileNavigationFixture.items}
            onCartClick={handleOnCartClick}
            onLoginClick={handleOnLoginClick}
            secondaryItems={MobileNavigationFixture.secondaryItems}
            theme={theme}
          />
          <HeroBanner
            backgroundColour
            hasTopOffset={true}
            copy={HeroBannerFixture.copy.default}
            media={
              <Video
                hasAllowAudio={false}
                hasAutoplay={true}
                hasLoop={true}
                hasPlayInFullScreen={false}
                hasControls={false}
                isFullWidth={false}
                sizes={{
                  small:
                    'https://player.vimeo.com/external/570830129.hd.mp4?s=760193620a08474db145098ae5a8dc499f82a651&profile_id=175',
                }}
              />
            }
          />
          <Modal
            copy={{
              close: 'Close Modal',
            }}
            isVisible={cartOpen}
            onClose={handleOnCartCloseClick}
          >
            You have 0 items in you cart.
          </Modal>
          <Modal
            copy={{
              close: 'Close Modal',
            }}
            isVisible={loginOpen}
            onClose={handleOnLoginCloseClick}
          >
            Login form
          </Modal>
          <StorybookGridOverlay {...implementation.gridOverlay} />
        </>
      );
    })()}
  </Story>
</Canvas>

## API

<ArgsTable of={MobileNavigation} />
